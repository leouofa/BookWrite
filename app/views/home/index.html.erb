<div class="w-full">
  <div class="flex justify-between items-center mb-8">
    <h1 class="font-bold text-4xl">Welcome to BookWrite</h1>
    <div class="text-right">
      <% if user_signed_in? %>
        <p class="mb-2">Logged in as: <strong><%= current_user.email %></strong></p>
        <%= button_to "Sign out", destroy_user_session_path, method: :delete, class: "bg-red-500 hover:bg-red-700 text-white font-bold py-2 px-4 rounded" %>
      <% end %>
    </div>
  </div>
  
  <div class="bg-white shadow-md rounded px-8 pt-6 pb-8 mb-4">
    <p class="mb-4">You are now successfully authenticated with Devise!</p>
    <p>This is your private dashboard where you can manage your content.</p>
  </div>
</div>
